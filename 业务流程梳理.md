# 业务流程梳理

> 这份文档记录「闪念 Pro」的真实业务流程，由使用者口述、对照代码实现整理而成。
> **目的**：在做任何界面改造之前，先把"业务是怎么跑的"和"代码是怎么写的"对齐，找出差异和缺口。
>
> 状态：**已核对确认**（2026-08-29）。
>
> 📌 **技术选型、容量规划、待办事项等见 `技术决策记录.md`**
> 📌 本文件只讲业务流程，技术问题看那边。

---

## 一、这个应用是什么

一个**面向短视频内容创作者的本地工作台**：从刷到好视频、拆解它、产生选题、写文案、录口播、发布、追数据、复盘，到最终沉淀成知识库，覆盖一条内容从灵感到复盘的完整生命周期。

**核心定位**：重内容创作。写文案是这个应用的重心，其余模块都是为它服务的。

---

## 二、谁在用（重要前提）

| 使用者 | 主要设备 | 主要职责 |
|--------|---------|---------|
| 峰岚（你） | 电脑 | 全流程，偏深度操作 |
| 小伙伴 | 手机 | 拆解对标是高频操作 |

代码里的证据：
- 多个模块硬编码了当前用户 `CURRENT_USER = "峰岚"`
- `Benchmark` 有 `assignee`（负责人）和 `assigneeHistory`（转让历史）
- `benchmark.ts` 有完整的**负责人转让**功能，转让后原负责人变为只读

⚠️ **但这里有一个架构级问题，见第五节「关键发现 1」**

---

## 三、完整业务流程（五阶段）

### 阶段 1 · 晨间规划

```
早上起来 → 今日待办 → 记下今天的目标
  例：今天拆解 100 条对标视频
  例：今天要发布 3 条内容
```

**要点**：待办是一天的起点，是任务的调度中心。

### 阶段 2 · 素材采集（快，批量）

```
刷视频 → 遇到好的 → 打开对标拆解模块
       → 只记录基础信息（标题 / 链接 / 视频文案 / 来源）
       → 不立刻拆解，先攒着
```

**要点**：这一步追求**快**，攒到一定数量再统一处理。

### 阶段 3 · 深度拆解（慢，集中）

```
打开之前记录的对标 → 对照视频文案 → 做四个维度分析

  ① 人群维度：这条视频是给什么样的人看的？
  ② 需求维度：在解决这群人的什么问题？
  ③ 内容维度：内容结构是怎么搭的？
  ④ 自身维度：我做得到吗？

拆解完 → 判断要不要转成新选题 → 沉淀进选题库
```

**要点**：这一步是深度思考，需要对照文案反复看。

### 阶段 4 · 内容生产 ★ 重中之重

```
新建待办（今天发布几条）
   ↓
打开内容生产流程，两种模式：

【即兴模式】先写文案，不管选题不管框架
   · 想到什么说什么，不被框架约束，即时性产出
   · 这一步不能被打扰
   · 写完之后 → 根据文案内容反向建立新选题
             → 再选一个合适的脚本框架
             → 把文案填进去

【标准模式】选题 → 框架 → 写文案（结构优先）

   ↓
文案 = 提词器 → 录口播 / 做素材
   ↓
素材模块打「完成」标记
   ↓
剪辑 → 发布
   ↓
制作发布模块：记录标题 / 描述 / 话题 / 文案 / 链接
```

**要点**：
- 即兴模式与标准模式的**顺序是反的**
- 文案同时充当**提词器**，**手机端和电脑端都要能当提词器用**
- ⚠️ **每发布一条视频，必录一条发布信息**——这是硬性规则，目的是便于后续追踪复盘

### 阶段 5 · 追踪与沉淀

```
发布后 → 数据追踪：选几个节点记录数据
         · 完播率怎么样
         · 跳出有没有降低，有没有把人留下
         · 观众参与度怎么样
   ↓
复盘（横向 + 纵向）
   · 复盘视频数据 → 找可优化点
   · 复盘工作流程、工作内容
   · 好的继续保持 → 不好的转成待办
   ↓
全部沉淀 → 大脑知识库（对齐 Obsidian）
```

---

## 四、各模块的定位

| 模块 | 定位（你的原话） | 在流程中的位置 |
|------|-----------------|--------------|
| 今日待办 | 负责任务 | 每天的起点，调度中心 |
| 闪念池 | 快记 | 随手记，之后归类 |
| 对标拆解 | 拆解 | 阶段 2 + 阶段 3 |
| 问答收集 | 一个问题多个答案 | 素材来源之一 |
| 灵感记录 | 灵感 + 思考过程 + 最终结论 | 素材来源之一 |
| 选题库 | 收纳灵感素材 | 阶段 3 的出口、阶段 4 的入口 |
| 脚本框架库 | 存储常用脚本结构 | 被内容生产引用 |
| **内容生产流程** | **写文案的地方（重中之重）** | **阶段 4** |
| 制作发布 | 记录发布后的标题/描述/话题等 | 阶段 4 末尾 |
| 数据追踪 | 发布后的数据追踪 | 阶段 5 |
| 复盘记录 | 复盘工作流程 + 视频数据 | 阶段 5 |
| 大脑知识库 | 所有知识沉淀，一比一对齐 Obsidian | 最终归档 |

### 模块之间的数据流向

```
闪念池 ──┐
问答收集 ┼──→ 选题库 ──┐
灵感记录 ─┘              │
对标拆解 ────────────────┤
                        ↓
                  内容生产流程 ←── 脚本框架库
                   （引用选题 + 引用脚本框架）
                        ↓
                    制作发布
                        ↓
                    数据追踪
                        ↓
                    复盘记录
                        ↓
        （上述所有模块的数据，统一同步到）
                        ↓
                   大脑知识库（存档）
```

**关键引用关系**：内容生产流程**引用**选题库的选题 + 脚本框架库的脚本框架——它自己不生产这两样，只消费。

---

## 五、对照代码后的关键发现

### 发现 1 ⚠️ 数据无法在两人之间同步（架构级问题）

**现状**：所有数据存在**各自浏览器本地**（IndexedDB，Dexie）。

**这意味着**：

```
你在电脑上创建了一条对标记录
   → 数据写进「你电脑的浏览器」
   → 转让给小伙伴
   → 只是在你的数据库里把负责人改成了他的名字
   → 小伙伴手机上一打开，他的浏览器里【根本没有这条记录】
```

**影响**：
- 「负责人转让」目前形同虚设——改了名字，但数据过不去
- 小伙伴在手机上看到的对标列表，和你电脑上的**完全不是同一份**
- 两人都做拆解，但成果无法汇总

**这是最严重的问题**，它比任何界面问题都重要。因为界面做得再好，两人看到的数据对不上，协作就不成立。

✅ **已决策（2026-08-29）**：采用**后端云数据库 + PWA 离线优先**方案。

| 项 | 决定 |
|---|---|
| 部署 | GitHub + Vercel（已在跑），域名 `app.fljytrain.cn` |
| 数据库 | **Turso**（9GB 免费，按实际量可用约 30 年） |
| 离线 | 保留 Dexie 本地层 + 加 PWA（断网也能用） |
| 账号 | 只两个固定账号，不需要注册页 |

> 详细技术方案、容量测算见 `技术决策记录.md`。

### 发现 2 你说的"四个维度"，代码里是五个

| 你说的 | 代码里的字段 |
|--------|------------|
| ① 人群维度 | `audienceIdentity` 身份 / `audienceStage` 阶段 / `audienceGoal` 目标 / `audiencePainPoint` 卡点 / `audienceEmotion` 情绪 |
| ② 需求维度 | `needsType` 类型 / `needsCategory` 划分 / `needsValue` 价值 / `coreProblem` 核心问题 |
| ③ 内容维度 | `presentationForm` 展现形式 / `structureType` 结构类型 / `structureSteps[]` 结构步骤 |
| ④ 自身维度 | `doable` 做得到吗 + `doableNote` / `thorough` 透不透 + `thoroughNote` / `diffLevel` 差异化 / `competition` 竞争热度 / `diffOpportunity` 机会点 |
| **⑤ 转化维度**（你没提） | `topicTitle` 最终凝练选题 / `topicCopy` 凝练出的文案 |

**判断**：第五个维度应该就是你说的「拆解完判断是否转选题」这一步——它既是拆解的最后一步，也是转选题的动作。

⚠️ **待确认**：你在电脑上拆解时，填完第四维度后，是不是还要填"凝练选题 + 凝练文案"这两项？还是说转选题是另外的操作？

### 发现 3 即兴模式的"反向流程"，代码已经完整支持 ✅

代码证据：
```js
// 创建即兴任务时，标记选题和框架为"待补填"
pendingStages = ["topic", "framework"]

// 推进到素材制作前，强制拦截检查
if (task.mode === "impromptu" && task.currentStage === "script") {
  if (!task.topicId || !task.frameworkId) {
    throw new Error("请先补填选题和脚本框架")
  }
}
```

并且有专门的 `createAndLinkTopic()` 函数实现「根据文案反向建选题」，还会把文案自动填入选题的 `copyReference` 字段。

**结论**：这条流程实现得**很完整**，跟你的描述完全吻合。

### 发现 4 提词器功能：代码里没有 ⚠️

生产流程的 `material`（素材制作）阶段，代码里的定义是"只读展示文案，按文案录口播/拍素材"——**只是一个文本展示区，不是提词器**。

你想要的是：
- 字要大
- 能自动滚动
- 速度可调

**目前完全不存在**，属于新功能。

### 发现 5 待办进度可以自动统计 ✅

代码里有完整的联动机制：
- 创建生产任务时，自动创建一条关联待办
- 每次推进阶段，自动同步进度（`syncProductionProgressToTodos`）
- Todo 有 `progressTargets` / `progressCompleted` / `linkedIds` 字段

**结论**：你记的「今天拆解 100 条」这类待办，理论上进度能自动算。

⚠️ **待确认**：你目前记"拆解 100 条"这种待办时，有没有去关联对标模块？如果没关联，进度就是手动的。

---

## 六、实施路线（已确认）

| 阶段 | 内容 | 完成后状态 |
|------|------|-----------|
| **1. 地基** | 推 GitHub + ID 改 UUID + 加同步字段 + 接入 PWA + 移除 7 处删除 | 应用照常可用，但具备同步与离线基础 |
| **2. 账号** | 登录页 + 两个账号 + 引导页 + 设置 / 用户中心 | 能登录，数据仍在本地 |
| **3. 同步** | Turso 接入 + 数据访问层 + 同步逻辑 | 联网自动同步，两人数据打通 |
| **4. 移动端** | 拆解向导 + 沉浸写作 + 提词器 | 手机端好用 |

**为什么"地基"必须排第一**：它改的是数据结构。现在改，改的是**代码**；上线后改，改的是**数据**（要写迁移脚本，风险高得多）。

这正是你「不怕现在麻烦，怕后续麻烦」原则的体现。

> 详细任务清单见 `技术决策记录.md` 第七节。

---

## 七、已确认事项（2026-08-29 更新）

| # | 问题 | 结论 |
|---|------|------|
| 1 | 数据同步怎么办？ | ✅ **用后端云数据库**，GitHub + Vercel + 自有域名 `app.fljytrain.cn`（已部署） |
| 2 | 拆解节奏 | ✅ **一次一条，绝不批量**（每个视频不同，必须细致）；但**会频繁中断续填** |
| 3 | 删除功能 | ✅ **全局无删除，一个不留**。数据都是灵感来源，后续接 AI 可调用 |
| 4 | 归档范围 | ✅ **只保留给待办模块**。其余模块是沉淀，不需要归档 |
| 5 | 移动端形态 | ✅ 做 **PWA**（不做 APP，因为需要 Mac 才能打包 iOS） |
| 6 | 每天拆解量 | ✅ **100 条是实际完成量**（非目标）。灵感沉淀数据量最大 |

### 仍待确认

| # | 事项 | 说明 |
|---|------|------|
| A | 第五个维度（转化）算不算拆解的一步？ | 代码有 5 维度，口述是 4 个。推测第五维度即"转选题"动作 |
| B | 图片需求 | 对标截图 / 知识库配图 / 灵感配图 / 发布封面，哪些需要？ |
| C | 提词器具体形态 | 大字号 + 自动滚动 + 调速，除此还要什么？ |
| D | 代码推 GitHub | 已部署但代码未推送，有丢失风险 |

---

## 八、附：流程与代码的吻合度总览

| 流程环节 | 代码实现 | 说明 |
|---------|---------|------|
| 晨间记待办 | ✅ 完整 | 支持优先级、截止日期、模块关联 |
| 对标快速录入 | ✅ 完整 | 基础信息字段齐全 |
| 对标四维度拆解 | ✅ 完整 | 五维度 40+ 字段，含转让功能 |
| 拆解转选题 | ✅ 完整 | 转化维度 + 转化联动 |
| 内容生产（标准） | ✅ 完整 | 六阶段流转 |
| 内容生产（即兴） | ✅ 完整 | 反向流程，代码支持到位 |
| **提词器** | ❌ **完全没有** | 仅文本展示 |
| 制作发布 | ✅ 完整 | 标题公式、标签库、3+1+1 结构 |
| 数据追踪 | ✅ 完整 | 2h/24h/3d/7d/30d 节点 |
| 复盘记录 | ✅ 完整 | 单条/周期，五模块结构 |
| 知识库 | ✅ 完整 | 节点/边/双向链接，对齐 Obsidian |
| **两人数据同步** | ❌ **不支持** | 各自浏览器本地存储 |
