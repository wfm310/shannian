# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/.next-build/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE / 编辑器本地数据（不提交，避免污染仓库）
.codebuddy/
.vscode/
.idea/
.trae/

# Windows 快捷方式（误入源码目录的垃圾文件）
*.lnk

# 提交信息临时文件（用于规避 PowerShell 中文参数编码问题）
.commit-msg.tmp

.vercel

# 构建验证临时目录（Next.js distDir，用 NEXT_DIST_DIR 环境变量指定）
# 注 1：IDE 安全删除策略会拦截大目录批量清理，故每次构建用新序号目录绕过
# 注 2：必须用通配 /.build-*/ 覆盖所有序号变体。此前只写了 .build-v*/
#      与 .build-tmp*/，导致 .build-w1 被误提交（641 个文件、1.7 万行）
/.build-*/
/.next*/

# Pixso 导出的样式系统总览（含 35MB 字体与生成代码，仅作色值参考，不入库）
/项目文档/闪念 Pro · 样式系统总览/
/项目文档/*.zip

# 旧文档目录（已被 项目文档/ 取代，待移出）
/项目文档整理/
