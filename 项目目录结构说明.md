# 项目目录结构说明

> 修改位置速查表 —— 想改什么，直接找对应目录

---

## 一、根目录文件（配置类）

| 文件 | 负责什么 | 什么时候改 |
|------|---------|-----------|
| `package.json` | 项目依赖、启动脚本 | 加新 npm 包、改启动命令 |
| `next.config.ts` | Next.js 配置 | 改构建配置、重写路由、加插件 |
| `tsconfig.json` | TypeScript 配置 | 改路径别名、TS 编译选项 |
| `components.json` | shadcn/ui 组件配置 | 改组件主题色、组件前缀 |
| `postcss.config.mjs` | PostCSS 配置 | 加 CSS 插件 |
| `eslint.config.mjs` | ESLint 配置 | 改代码检查规则 |
| `.gitignore` | Git 忽略文件 | 加不需要提交的文件 |
| `public/` | 静态资源（图片、图标等） | 放 logo、favicon、图片 |
| `AGENTS.md` | AI 编码助手规则（由 Next.js 自动生成，勿手改） | 基本不用动 |
| `CLAUDE.md` | 只有一行 `@AGENTS.md`，是给 Claude Code 的转发文件 | 可删可留 |

---

## 二、`src/app/` — 页面路由（改页面入口）

每个文件夹 = 一个页面路由，`page.tsx` 是页面入口。

| 目录 | 页面 | 路由地址 |
|------|------|---------|
| `page.tsx` | 首页（内容工作台） | `/` |
| `todo/page.tsx` | 今日待办 | `/todo` |
| `flash-thought/page.tsx` | 闪念池 | `/flash-thought` |
| `benchmark/page.tsx` | 对标拆解 | `/benchmark` |
| `topic-library/page.tsx` | 选题库 | `/topic-library` |
| `qa-collect/page.tsx` | 问答收集 | `/qa-collect` |
| `inspiration/page.tsx` | 灵感库 | `/inspiration` |
| `script-template/page.tsx` | 脚本模板库 | `/script-template` |
| `produce-flow/page.tsx` | 内容生产流程 | `/produce-flow` |
| `publish/page.tsx` | 制作发布 | `/publish` |
| `dashboard/page.tsx` | 数据追踪 | `/dashboard` |
| `review/page.tsx` | 复盘记录 | `/review` |
| `knowledge-base/page.tsx` | 大脑知识库 | `/knowledge-base` |
| `notification/page.tsx` | 消息通知 | `/notification` |
| `ai-module/page.tsx` | AI 模块 | `/ai-module` ⚠️ **目录为空，无 page.tsx，访问会 404** |

**全局布局文件**：
| 文件 | 负责什么 |
|------|---------|
| `layout.tsx` | 根布局（HTML 结构、全局 Provider、Tab 栏） |
| `globals.css` | 全局样式（CSS 变量、字体、重置样式、iOS HIG 令牌） |
| `favicon.ico` / `icon.svg` | 网站图标 |

---

## 三、`src/components/` — 组件（改 UI 样式和交互）

### 3.1 `components/ui/` — 全局共享 UI 组件（shadcn/ui）

改这些 = 全局生效，所有模块都会变。
**共 62 个组件**，其中 27 个已被业务代码使用，35 个为组件库预置、暂未使用（保留备用，一般不需要动）。

#### 已在使用（27 个，改动影响面大）

| 组件 | 用途 | 常用修改点 | 引用次数 |
|------|------|-----------|---------|
| `button.tsx` | 按钮 | 圆角、字号、颜色、按下效果 | 49 |
| `badge.tsx` | 标签徽章 | 圆角、字号、颜色变体 | 35 |
| `dialog.tsx` | 居中弹窗 | 动画、遮罩、圆角 | 27 |
| `input.tsx` | 输入框 | 圆角、背景、字号 | 21 |
| `card.tsx` | 卡片 | 圆角、阴影、内边距、字号 | 20 |
| `skeleton.tsx` | 骨架屏 | 动画效果 | 18 |
| `sheet.tsx` | 底部/侧边弹出层 | 圆角、动画、遮罩 | 15 |
| `textarea.tsx` | 文本域 | 圆角、背景、字号 | 15 |
| `select.tsx` | 下拉选择 | 样式 | 11 |
| `label.tsx` | 表单标签 | 字号、间距 | 10 |
| `tabs.tsx` | 标签切换 | 圆角、背景、选中态 | 6 |
| `separator.tsx` | 分隔线 | 颜色、粗细 | 4 |
| `sidebar.tsx` | 侧边栏基座 | 宽度、折叠动画 | 3 |
| `table.tsx` | 表格 | 行高、边框 | 3 |
| `dropdown-menu.tsx` | 下拉菜单 | 圆角、行高 | 3 |
| `checkbox.tsx` | 复选框 | 样式、大小 | 2 |
| `tooltip.tsx` | 气泡提示 | 样式、延迟 | 2 |
| `popover.tsx` | 弹出面板 | 样式、位置 | 2 |
| `chart.tsx` | 图表（recharts 封装） | 配色、图例 | 2 |
| `switch.tsx` | 开关 | 颜色、大小 | 2 |
| `calendar.tsx` | 日历组件 | 样式、日期格式 | 1 |
| `pagination.tsx` | 分页 | 样式 | 1 |
| `avatar.tsx` | 头像 | 大小、圆角 | 1 |
| `alert-dialog.tsx` | 确认对话框 | 样式、按钮排列 | 1 |
| `alert.tsx` | 告警条 | 配色 | 1 |
| `resizable.tsx` | 可调整大小面板 | 拖拽手柄 | 1 |
| `drawer.tsx` | 抽屉 | 动画、方向 | 1 |
| `wheel-date-picker.tsx` | 滚轮日期选择 | 样式 | 1 |

#### 暂未使用（35 个，组件库预置，留着备用即可）

`accordion` `aspect-ratio` `attachment` `breadcrumb` `bubble` `button-group` `carousel` `collapsible` `combobox` `command` `context-menu` `direction` `empty` `field` `hover-card` `input-group` `input-otp` `item` `kbd` `marker` `menubar` `message` `message-scroller` `native-select` `navigation-menu` `progress` `questionnaire` `radio-group` `scroll-area` `slider` `spinner` `toast` `toggle` `toggle-group`

> 说明：全局消息提示实际用的是 `sonner`（见 `components/toaster-provider.tsx`），`ui/toast.tsx` 目前没被用到。

### 3.2 `components/layout/` — 布局组件

| 文件 | 负责什么 |
|------|---------|
| `page-header.tsx` | 页面顶部导航栏（标题、描述、右侧按钮、搜索） |
| `responsive-layout.tsx` | 响应式布局容器（安全区、内边距） |
| `user-avatar.tsx` | 用户头像组件 |
| `theme-toggle.tsx` | 主题切换按钮 |
| `topbar.tsx` | 桌面端顶部栏（由 `responsive-layout` 引入，内含主题切换与用户头像） |

### 3.3 `components/mobile/` — 移动端专属

| 文件 | 负责什么 |
|------|---------|
| `mobile-tab-bar.tsx` | 底部 Tab 栏（13 个 Tab + 快记按钮） |

### 3.4 业务组件（按模块分）

每个模块一个文件夹，包含卡片、表单、详情等。

| 目录 | 模块 | 包含文件 |
|------|------|---------|
| `todo/` | 今日待办 | `todo-board-view` 看板 / `todo-calendar-view` 日历 / `todo-gantt-view` 甘特 / `todo-detail` 详情 / `todo-form` 表单 |
| `flash-thought/` | 闪念池 | `flash-thought-detail` 详情 / `flash-thought-d3-graph` 网络图 / `flash-thought-action` 操作按钮 / ~~`flash-thought-card` 卡片~~ ⚠️未使用 / ~~`flash-thought-input` 输入框~~ ⚠️未使用 （无 `flash-thought-form`，新建走页面内联表单） |
| `benchmark/` | 对标拆解 | `benchmark-workspace` 工作区（主入口）/ `benchmark-list` 列表 / `benchmark-detail` 详情 / `benchmark-form` 表单 / `benchmark-transfer` 转存 / `benchmark-convert` 转换 / ~~`benchmark-table` 表格~~ ⚠️未使用 / ~~`benchmark-kanban` 看板~~ ⚠️未使用 |
| `topic/` | 选题库 | `topic-card` 卡片 / `topic-detail` 详情 / `topic-form` 表单 |
| `qa-collect/` | 问答收集 | `qa-card` 卡片 / `qa-detail` 详情 / `qa-form` 表单 |
| `inspiration/` | 灵感库 | `inspiration-card` 卡片 / `inspiration-detail` 详情 / `inspiration-form` 表单 |
| `script-template/` | 脚本模板库 | `script-template-card` 卡片 / `script-template-detail` 详情 / `script-template-form` 表单 |
| `produce-flow/` | 内容生产流程 | `production-card` 卡片 / `production-detail` 详情 / `production-form` 表单 |
| `publish/` | 制作发布 | `publish-card` 卡片 / `publish-detail` 详情 / `publish-form` 表单 |
| `tracking/` | 数据追踪 | `tracking-overview` 概览 / `tracking-detail` 详情 / `tracking-video-detail` 视频详情 / `tracking-form` 表单 / ~~`tracking-card` 卡片~~ ⚠️未使用 |
| `review/` | 复盘记录 | `review-card` 卡片 / `review-detail` 详情 / `review-form` 表单 |
| `knowledge-base/` | 大脑知识库 | `knowledge-graph` 知识图谱 / `knowledge-node-detail` 节点详情 / ~~`knowledge-note-editor` 笔记编辑器~~ ⚠️未使用 |
| `notification/` | 消息通知 | `notification-detail` 详情 / `notification-dropdown` 下拉 |

### 3.5 其他全局组件

| 文件 | 负责什么 |
|------|---------|
| `app-sidebar.tsx` | 桌面端侧边栏 |
| `page-transition.tsx` | 页面切换动画 |
| `quick-flash-thought.tsx` | 快记（快速闪念）弹窗 |
| `toaster-provider.tsx` | 消息提示 Provider |

---

## 四、`src/lib/` — 数据层（改数据逻辑）

所有本地数据库操作、业务逻辑都在这里。用 Dexie.js（IndexedDB 封装）。

| 文件 | 负责什么 | 改什么找它 |
|------|---------|-----------|
| `db.ts` | 数据库定义（所有表结构、类型定义） | 加新表、改字段、改索引 |
| `utils.ts` | 通用工具函数 | 加通用工具方法 |
| `priority.ts` | 优先级计算逻辑 | 改评分公式 |
| `flash-thought.ts` | 闪念 CRUD | 改闪念数据操作 |
| `benchmark.ts` | 对标拆解 CRUD | 改对标数据操作 |
| `topic.ts` | 选题库 CRUD | 改选题数据操作 |
| `qa-collect.ts` | 问答收集 CRUD | 改问答数据操作 |
| `inspiration.ts` | 灵感库 CRUD | 改灵感数据操作 |
| `script-template.ts` | 脚本模板 CRUD | 改脚本模板数据操作 |
| `produce-flow.ts` | 生产流程 CRUD | 改生产流程数据操作 |
| `publish.ts` | 发布记录 CRUD | 改发布数据操作 |
| `tracking.ts` | 数据追踪 CRUD + KPI 计算 | 改追踪数据和统计逻辑 |
| `review.ts` | 复盘记录 CRUD | 改复盘数据操作 |
| `knowledge-base.ts` | 知识库 CRUD | 改知识库数据操作 |
| `notification.ts` | 消息通知 CRUD | 改通知数据操作 |
| `notification-events.ts` | 通知事件发送 | 改通知触发逻辑 |
| `progress-events.ts` | 进度事件订阅 | 改进度更新广播 |

> ⚠️ 待办模块**没有独立的 `lib/todo.ts`**，它的增删改查全部写在 `src/app/todo/page.tsx` 里，直接操作 `db.todos`。要改待办数据逻辑就找这个文件。

---

## 五、`src/hooks/` — 自定义 Hooks（改通用逻辑）

| 文件 | 负责什么 |
|------|---------|
| `use-mobile.ts` | 判断是否移动端 |
| `use-media-query.ts` | 媒体查询 Hook |
| `use-auto-save.ts` | 自动保存 Hook（表单草稿） |
| `use-delayed-loading.ts` | 延迟加载 Hook |

---

## 六、修改位置速查表

### 改视觉样式

| 想改什么 | 找哪里 |
|---------|--------|
| 全局颜色、字体、圆角、阴影令牌 | `src/app/globals.css` |
| 按钮样式 | `src/components/ui/button.tsx` |
| 卡片样式 | `src/components/ui/card.tsx` |
| 输入框样式 | `src/components/ui/input.tsx` |
| 底部弹出层样式 | `src/components/ui/sheet.tsx` |
| 弹窗样式 | `src/components/ui/dialog.tsx` |
| Tab 切换样式 | `src/components/ui/tabs.tsx` |
| 页面顶部导航栏 | `src/components/layout/page-header.tsx` |
| 底部 Tab 栏 | `src/components/mobile/mobile-tab-bar.tsx` |
| 全局布局/安全区 | `src/components/layout/responsive-layout.tsx` |

### 改页面内容

| 想改什么 | 找哪里 |
|---------|--------|
| 首页布局/内容 | `src/app/page.tsx` |
| 某个模块的页面结构 | `src/app/[模块名]/page.tsx` |
| 某个模块的卡片样式 | `src/components/[模块]/xxx-card.tsx` |
| 某个模块的详情弹窗 | `src/components/[模块]/xxx-detail.tsx` |
| 某个模块的新建/编辑表单 | `src/components/[模块]/xxx-form.tsx` |

### 改数据/业务逻辑

| 想改什么 | 找哪里 |
|---------|--------|
| 加新字段/改表结构 | `src/lib/db.ts` |
| 某个模块的数据操作 | `src/lib/[模块].ts` |
| 评分/计算公式 | `src/lib/priority.ts` 或对应模块 lib |
| 消息通知触发 | `src/lib/notification-events.ts` |
| 进度更新广播 | `src/lib/progress-events.ts` |

### 改配置

| 想改什么 | 找哪里 |
|---------|--------|
| 项目依赖/启动命令 | `package.json` |
| Next.js 构建配置 | `next.config.ts` |
| TypeScript 配置 | `tsconfig.json` |
| shadcn 组件配置 | `components.json` |
| 静态资源 | `public/` |

---

## 七、待清理清单（代码体检结果）

按优先级排列，都是安全可做的整理项：

| # | 问题 | 位置 | 建议 |
|---|------|------|------|
| 1 | 空目录，但通知模块已经跳转到 `/ai-module`，点进去会 404 | `src/app/ai-module/`（目录内无任何文件） | 要么补 `page.tsx`，要么删目录并去掉 `notification-detail.tsx` / `notification/page.tsx` / `lib/db.ts` 里的 `ai-module` 引用 |
| 2 | Windows 快捷方式文件混进了源码目录 | `src/app/icon.svg.lnk` | 直接删除（真正的图标是同目录的 `icon.svg`） |
| 3 | 6 个组件写好了但从未被引用（死代码） | `components/benchmark/benchmark-table.tsx`<br>`components/benchmark/benchmark-kanban.tsx`<br>`components/flash-thought/flash-thought-card.tsx`<br>`components/flash-thought/flash-thought-input.tsx`<br>`components/tracking/tracking-card.tsx`<br>`components/knowledge-base/knowledge-note-editor.tsx` | 确认不再需要就删掉；想保留也建议加注释说明用途 |
| 4 | 35 个 shadcn 组件装了但没用上 | `components/ui/`（见 3.1 第二节清单） | 留着无害，只是 `bundle` 里不会有它们，可不动 |
| 5 | README 还是 `create-next-app` 的默认模板，没写项目信息 | `README.md` | 建议替换成本项目的启动说明（dev 端口是 **3001**，不是文档里写的 3000） |
| 6 | 数据层不统一：其他模块都有 `lib/xxx.ts`，只有待办把 CRUD 写在页面里 | `src/app/todo/page.tsx` | 想统一的话，把 `db.todos` 操作抽成 `lib/todo.ts` |
| 7 | 编译产物已正确忽略 | `tsconfig.tsbuildinfo`、`next-env.d.ts` | 已在 `.gitignore` 中，无需处理 ✅ |

### 文件数量总览

- 页面路由：15 个（14 个有 `page.tsx`，`ai-module` 为空）
- 组件：121 个 `*.tsx`（`ui/` 62 个 + 业务/布局 59 个）
- 数据层：17 个 `*.ts`（`src/lib/`）
- 自定义 Hook：4 个（`src/hooks/`）
