# 技术决策记录

> ⚠️ **重要：新开对话时，请先读这个文件**，它能让你快速恢复全部上下文。
> 配套文档：`业务流程梳理.md`（业务怎么跑）、`项目目录结构说明.md`（代码在哪）
>
> 最后更新：2026-08-29（阶段一全部完成）

---

## 零、一句话总览

一个**离线优先 + 云端同步**的双人协作内容创作工作台。
技术主线：保留现有本地架构 → 加 PWA → 加云端同步 → 重做移动端。

---

## 一、项目定位

短视频内容创作者的本地工作台，覆盖「刷到好视频 → 拆解 → 选题 → 写文案 → 录口播 → 发布 → 追数据 → 复盘 → 沉淀知识库」的完整生命周期。

**核心定位**：重内容创作。**写文案是这个应用的重心**，其余模块都为它服务。

---

## 二、使用者（关键前提）

| 使用者 | 设备 | 职责 |
|---|---|---|
| 峰岚（你，需求方） | 电脑为主 | 全流程，深度操作 |
| 小伙伴 | 手机为主 | **拆解对标是高频操作** |

代码里多处硬编码 `CURRENT_USER = "峰岚"`，**这是用户昵称占位符，不是真实账号**——后续要换成真实账号体系。

### 每日实际工作量（容量规划依据）

- **对标拆解：每天 100 条**（实际完成量，非目标）
- 灵感沉淀是**数据量最大**的模块
- 另有闪念、问答、选题、内容生产、发布、追踪、复盘、知识库等模块日常使用

**这个量级对录入效率和列表性能提出了很高要求**（见第五节）。

---

## 三、用户定下的原则（必须遵守）

1. **「不怕现在麻烦，怕后续麻烦」**
   → 凡是"现在不改、上线后必炸"的问题，一律现在改。宁可现在多花功夫。

2. **「不能上线半成品」**
   → 上线必须是完整可用版，否则会分散内容创作的精力，反而降低产出。

3. **「全局无删除」**
   → 所有数据都是灵感来源和思考过程，后续接入 AI 模块可直接调用。
   → **可以归档，不能删除。**

4. **「现在多花功夫做好基础，后续才省麻烦」**
   → 技术选型优先考虑长期影响，而非短期省事。

---

## 四、已确认的决策

### 4.1 业务决策

| # | 决策 | 说明 |
|---|---|---|
| 1 | **全局无删除，一个不留** | 需清理现有 7 处删除功能（详见第七节） |
| 2 | **归档只保留给待办模块** | 理由：任务是会过期的，每天醒来一堆任务很头大；灵感/选题/复盘等是沉淀，越攒越有成就感 |
| 3 | **对标拆解：一次一条，绝不批量** | 每个视频内容不同，必须细致一条条拆 |
| 4 | **但会频繁中断续填** | 拆一半退出、过会儿接着拆是常态，向导必须支持断点续填 |
| 5 | **快记不做归类** | 快记只记录，归类是之后在闪念池做的事（见 4.4） |
| 6 | **不做 APP，做 PWA** | 用户是 Windows，iOS APP 需要 Mac |

### 4.2 技术决策

| # | 决策 | 理由 |
|---|---|---|
| 7 | **继续用 Vercel，不换 Cloudflare** | 用户已在 Vercel 部署且能用；Next.js 是 Vercel 自家产品配合最好；**PWA 让日常使用不受服务器位置影响** |
| 8 | **做 PWA（Service Worker）** | 断网也能打开应用（数据本就在本地，但页面文件需要缓存） |
| 9 | **保留 Dexie 作为本地层** | 现有架构天生就是离线优先，不用推翻重来 |
| 10 | **数据库选 Turso（9GB）** | Supabase 500MB 按用户实际量只够 1.8 年；Turso 够用 30 年；且 Turso 是 SQLite，与 Dexie 同体系，同步开发更顺 |
| 11 | **加"数据访问层"抽象** | 让将来换数据库只改一处，业务代码不动。**这层本来就要做**（本地+云端调度），顺带实现 |
| 12 | **记录 ID 改为全局唯一（UUID）** | 现有 `++id` 自增会导致两人数据编号冲突 |
| 13 | **账号只需两个，不需要注册页** | 后台预建两个账号，前端只做登录 |
| 14 | **不做软删除** | 因全局无删除，同步层只需处理"新增+更新"，逻辑大幅简化 |

### 4.3 待确认（尚未最终拍板）

| # | 事项 | 状态 |
|---|---|---|
| A | **图片/文件需求** | ⚠️ 未定。用户被问到「对标截图 / 知识库配图 / 灵感配图 / 发布封面」哪些需要，**尚未回答** |
| B | Turso 国内访问速度 | 需实测（建议做最小测试页对比 Turso 与 Cloudflare D1） |
| C | 代码推 GitHub | 用户已部署但**代码未推送**，有丢失风险。提议过，用户未明确回应 |
| D | 对标拆解是否算"第五维度" | 代码有 5 个维度（含"转化维度"= 凝练选题+文案），用户口述是 4 个。推测第五维度即"转选题"动作，待确认 |

### 4.4 已纠正的错误决策（重要，避免重犯）

**快记曾加过"闪念/灵感/问答"三选一，已按用户意见移除。**

- 我的错误：看到三个接口现成，就让用户一次到位选归属
- 用户的指正：快记定位是"随手记，想到什么记什么"，**归类是之后在闪念池做的事**
- 代码证据：`flash-thought.ts` 的 `categorizeFlashThought` 注释明确写着「延迟状态变更」
- 额外问题：直接存成灵感会缺少 `thoughtProcess`/`conclusion`，导致记录永远残缺

**教训：涉及产品定位的判断，用户永远是对的。我的职责是把判断落地，不是替他决策。**

---

## 五、数据容量规划

### 文字数据（纯字段）

| 模块 | 估算 | 年增量 |
|---|---|---|
| 对标拆解（100 条/天 × 6KB） | | **219MB** |
| 灵感（三段式，量最大） | 20 条/天 | ~22MB |
| 闪念 | 30 条/天 | ~5MB |
| 问答 | 10 条/天 | ~7MB |
| 选题 | 5 条/天 | ~5MB |
| 内容生产 + 发布 | 6 条/天 | ~11MB |
| 知识库笔记 | 2 条/天 | ~4MB |
| 追踪 + 复盘 | | ~1MB |
| **合计** | | **≈ 274MB/年** |

**数据库容量对照**：
| 数据库 | 容量 | 能撑 |
|---|---|---|
| Supabase | 500MB | ~1.8 年 ❌ |
| Cloudflare D1 | 5GB | ~16 年 |
| **Turso（选定）** | **9GB** | **~30 年** ✅ |

### 图片数据（待确认）

**当前代码完全不存图片**——`db.ts` 只有两个 `videoUrl` 外链字符串，无上传功能，`ui/attachment.tsx` 从未被使用。

若将来需要，图片走**对象存储**（不与数据库共用容量）：

| 服务 | 免费容量 | 关键差异 |
|---|---|---|
| Supabase Storage | 1GB | 流量受限 |
| **Cloudflare R2** | **10GB** | **出站流量免费**（图片反复加载，流量才是大头） |

**图片会让容量质变**：若每条对标配 1 张压缩截图（300KB），一年就是 **11GB**，免费对象存储只够 1 年。

**决策：架构上预留文件存储接口（现在零成本），是否实际接入取决于需求确认。**

---

## 六、关于"国内访问速度"的重要判断

原本担心国外服务器国内访问慢，但**PWA 改变了这个判断**：

```
首次打开：从服务器加载（可能慢，仅此一次）
     ↓
之后每次：页面已在本地，秒开，不联网
     ↓
数据同步：后台进行，慢半秒无感知
```

**结论：日常使用体验与服务器位置几乎无关。** 用户实测流量打开 3~5 秒，可接受。

---

## 七、待执行的清理工作

### 7.1 移除 7 处删除功能（全局无删除）

| 文件 | 行 | 内容 |
|---|---|---|
| `lib/tracking.ts` | 174 | 删除追踪记录 |
| `lib/script-template.ts` | 164 | 删除脚本框架 |
| `lib/publish.ts` | 243 | 删除发布记录 |
| `lib/publish.ts` | 315 | 删除标签 ⚠️ **标签库确实存在，见 7.3** |
| `lib/produce-flow.ts` | 343 | 删除生产任务 |
| `lib/knowledge-base.ts` | 545 | 删除知识笔记 |
| `lib/benchmark.ts` | 441 | **批量**删除对标记录 |
| `components/benchmark/benchmark-table.tsx` | 1194-1206 | 删除确认弹窗（写着"不可撤销"） |

### 7.2 标签库澄清

**用户以为项目里没有标签库，实际是有的**，只是没有独立入口：

```
制作发布 → 打开某条发布记录 → 选「话题标签」处
        → 展示常用标签，按 赛道/内容/人群/热点 分类（即"3+1+1"结构）
```

| 位置 | 内容 |
|---|---|
| `db.ts:503` | `TagLibrary` 结构（标签、分类、使用次数） |
| `publish.ts:270` | 读取、自动统计使用次数 |
| `publish-detail.tsx:351` | 按分类展示、点击快速选用 |

### 7.3 其他遗留问题（之前梳理发现，未处理）

| 问题 | 位置 |
|---|---|
| 空目录导致 404 | `src/app/ai-module/` 空，但 3 处引用 `/ai-module` |
| Windows 快捷方式混入 | `src/app/icon.svg.lnk` |
| 6 个零引用死组件 | `benchmark-table` `benchmark-kanban` `flash-thought-card` `flash-thought-input` `tracking-card` `knowledge-note-editor` |
| README 是默认模板 | 端口写成 3000，实际是 **3001** |

---

## 八、已完成的工作（2026-08-29）

### 8.1 移动端快记（已完成并验收通过）

| 文件 | 说明 |
|---|---|
| `src/lib/breakpoints.ts` | 新建。断点常量唯一来源（解决 768/1024 冲突） |
| `src/hooks/use-visual-viewport.ts` | 新建。键盘高度探测（iOS Safari 唯一可靠方案） |
| `src/components/mobile/ui/ios-sheet.tsx` | 新建。底部弹层：下滑关闭 + 键盘避让 |
| `src/components/mobile/quick-capture-sheet.tsx` | 新建。快记：打开即输入 + 防抖草稿 + 无归类 |
| `src/components/quick-flash-thought.tsx` | 修改。移动端分支改挂新组件，桌面端**零改动** |
| `src/app/page.tsx` | 修改。数据加载失败不再永久卡骨架屏 |

**验收结果**：用户用 F12 手机模式测过 8 项，全部通过。

### 8.2 修复的隐患

- 断点冲突（768 与 1024 之间两个 hook 都返回 false）
- 弹层在渲染期间改 state（可能导致 hydration 异常）
- 首页数据读不出会永久卡在骨架屏

### 8.3 阶段一·地基改造（已完成并通过 tsc 零错误 + next build 验证）

| 项目 | 说明 |
|---|---|
| **ID 改 UUID（决策 12）** | 所有实体主键从 `++id` 自增改为 `string` UUID（`newId()` 基于 `crypto.randomUUID` 兜底）。涉及 `db.ts` 全部接口、`id.ts`、`12` 个 `lib` 文件（`create` 用 `newId()`）、约 40 个组件（state / `parseInt` / 回调参数 / 本地类型注解） |
| **表结构加同步字段（决策预备）** | 所有实体接口统一新增三件套：`synced: boolean`、`updatedAt: number`、`syncVersion: number`（乐观并发）；新增 `SyncMeta` 表（记录最后同步时间）；`db` 升级到 `version 13`。`id.ts` 提供 `newSyncFields()`（新建初始值）与 `touchSyncFields(v)`（更新时递增版本）。所有 `lib` 的 `add` 注入 `...newSyncFields()`，`update` 注入 `...touchSyncFields(当前版本)` |
| **接入 PWA（决策 6/8）** | 新增 `public/manifest.webmanifest`、`public/sw.js`（network-first 导航 + stale-while-revalidate 静态资源，业务数据走 IndexedDB 不受影响）、`public/icon-192.svg` / `icon-512.svg`；新增 `src/components/sw-register.tsx`（仅生产环境注册 SW）；`layout.tsx` 补充 `manifest` / `appleWebApp` / `icons` 元数据与 `themeColor` viewport |

**验证结果**：`tsc --noEmit` 零错误；`next build` 14 个页面路由全部编译成功；PWA 资源经 `next start` 由 `public/` 文件系统路由在运行时提供（`useFileSystemPublicRoutes: true`）。

**关键约定（后续同步阶段必须遵守）**：
- 任何写操作都必须维护同步字段——新建用 `newSyncFields()`，更新用 `touchSyncFields(current.syncVersion || 0)`。
- `synced` 标记是否需要上云：`newSyncFields()` 初始为 `false`；同步层将其置 `true`。
- 全局无删除（决策 1），同步层只需处理「新增 + 更新」，无软删除逻辑。

### 8.4 修复：IndexedDB 主键不可变导致的升级崩溃（DatabaseClosedError: Not yet support for changing primary key）

**现象**：用户浏览器持有旧版数据库（`shannian-pro`，v1~v11，自增数字主键 `++id`）时，打开应用直接崩溃，报 `UpgradeError: Not yet support for changing primary key`。开发环境因清过 IndexedDB 未暴露此问题。

**根因**：Dexie 升级时，`getSchemaDiff` 检测到旧库主键（`++id`，outbound）与新主键（`id`，inline）不同 → 判定需要 `recreate` object store → IndexedDB 原生禁止修改已有 store 的主键 → 抛错。任何从旧库升级的用户都会踩中，与 version 12/13 的写法无关。

**修复**（一次性、彻底）：
- 数据库名由 `shannian-pro` 改为 **`shannian-pro-v2`**，从 `version(1)` 直接以最终 schema 建库（UUID 字符串主键 + 同步字段索引 + `syncMeta` 表），彻底绕开 upgrade 路径里的主键冲突。
- 删除了 version 2~13 的全部历史 `.stores()` 堆积，schema 只剩一个干净的 `version(1)`。
- 打开新库成功后，自动 `Dexie.delete("shannian-pro")` 清理旧库残留（失败静默忽略）。

**约定**：IndexedDB 主键一旦建库便不可更改。今后若需再次变更主键，**只能换数据库名**重建，不能用 upgrade 改主键。新增表/索引可用更高 version 的 `version(n).stores()` 增量升级（不触碰主键即可）。

---

## 九、后续路线图

| 阶段 | 内容 | 完成后状态 |
|---|---|---|
| **1. 地基** ✅ | 推 GitHub + ID 改 UUID + 表结构加同步字段 + 接入 PWA（+ 移除 7 处删除待单独执行，见 7.1） | 应用和现在一样能用，且具备同步与离线基础（tsc 零错误、next build 通过） |
| **2. 账号** | 登录页 + 两个账号 + 引导页 + 设置 / 用户中心 | 能登录，数据仍在本地 |
| **3. 同步** | Turso 接入 + 数据访问层 + 同步逻辑 | 联网自动同步，两人数据打通 |
| **4. 移动端** | 拆解向导 + 沉浸写作 + 提词器 | 手机端好用 |

> **注**：阶段一中的「移除 7 处删除」（`7.1`）尚未执行，属于独立清理项，可单独安排。UUID、同步字段、PWA 三项均已落地。

**为什么第一阶段必须先做**：它改的是数据结构（UUID、同步字段），**越晚做代价越大**——现在改的是代码，上线后改的是数据（要写迁移脚本）。

---

## 十、移动端各场景设计要点（已确认）

| 场景 | 设计要点 |
|---|---|
| **闪念快记**（已完成） | 打开即输入、下滑关闭、键盘避让、自动存草稿。**不做归类** |
| **对标拆解** | 4 步向导（人群/需求/内容/自身），**一次一条不做批量**；每次打开自动回到上次步骤；每字段改完即存；顶部显示完成进度 |
| **即兴模式写作** | **一整块空白，什么都不显示**——没有步骤、没有引导语，完全自由，不被打扰 |
| **标准模式写作** | 按脚本框架分步骤写（与即兴模式完全不同，需区分对待） |
| **提词器** | 新功能，代码里完全没有。需：大字号 + 自动滚动 + 速度可调 |

---

## 十一、协作方式（用户认可的）

> **用户负责「对不对」（业务判断），我负责「好不好看、跑不跑得通」（实现）。**

用户自述："我不懂代码，不懂 UI/UX，但我最懂我的业务流程。"

**已被验证有效**：用户一句话纠正了我的"快记三选一"设计错误，指出其违背产品定位。
**结论：涉及产品定位的判断，优先听用户的；我的职责是把判断落地并指出技术风险。**
